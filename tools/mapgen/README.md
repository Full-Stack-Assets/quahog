# tools/mapgen — ⚠️ legacy (Unity-targeted)

OSM → GeoJSON fetcher written for the legacy Unity `GisCity` renderer
(`QUAHOG_Unity/Assets/Resources/gis/newbedford.json`).

**The canonical map pipeline for the web game is
[`quahog-project-files/mapdata/`](../../quahog-project-files/mapdata/)** — see
[`../../ENGINES.md`](../../ENGINES.md). Both pull from the same Overpass source;
this one is kept for reference with the Unity track.
